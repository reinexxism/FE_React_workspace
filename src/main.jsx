import React from "react";
import ReactDOM from "react-dom/client";
import AppTravelList from "./app/AppTravelList";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppTravelList />
  </React.StrictMode>
);
